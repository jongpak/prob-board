{% extends 'default/layout.twig' %}

{% block body %}
<h2>Boards</h2>
<ul>
    {% for board in boards %}
    <li>
        <a href="{{ url(board.getName()) }}">
            {{ board.getTitle() }}
        </a>
    </li>
    {% endfor %}
</ul>
{% endblock %}
