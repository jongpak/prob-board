{% if not loggedAccountId %}
<form method="POST" action="{{ url('/auth/login') }}" class="form-inline">
    <div class="form-group">
        <label class="sr-only">ID</label>
        <input type="text" name="account_id" value="" placeholder="ID" class="form-control input-sm" />
    </div>
    <div class="form-group">
        <label class="sr-only">Password</label>
        <input type="password" name="password" value="" placeholder="Password" class="form-control input-sm" />
    </div>
    <div class="form-group">
        <input type="submit" value="Login" class="btn btn-primary btn-sm" />
        <a href="{{ url('/auth/register') }}">Register</a>
    </div>
</form>

{% else %}
<p>
    User: {{ loggedAccountId }}
    <a class="btn btn-warning btn-sm" href="{{ url('/auth/logout') }}">Logout</a>
</p>
{% endif %}
